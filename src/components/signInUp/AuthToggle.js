import { View, Text, Pressable } from 'react-native';
import Style from '~/styles/SignInUpStyle';

const AuthToggle = (props) => {
  return <View style={Style.toggle_container}></View>;
};

export default AuthToggle;
